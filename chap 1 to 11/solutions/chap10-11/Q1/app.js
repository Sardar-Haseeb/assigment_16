    // Prompt the user to enter the city name
    var cityName = prompt("Enter the name of your city:");

    // Check if the entered city is "Karachi" and display a welcome message
    if (cityName && cityName.toLowerCase() === "karachi") {
        alert("Welcome to the city of lights, Karachi!");
    }